# Metabolite Matcher 使用指南

## 📋 完整使用步骤

### 步骤1: 安装Python依赖包

在项目目录下打开命令行（终端），运行：

```bash
pip install -r requirements.txt
```

**预期输出**：
```
Collecting openai>=1.12.0
Collecting requests>=2.31.0
...
Successfully installed openai-1.x.x requests-2.x.x
```

---

### 步骤2: 准备输入文件

从MetaboAnalyst下载的CSV文件应该已经在当前目录，名为 `name_map.csv`。

**文件格式检查**：
- 必须包含列：`Query`, `Match`, `HMDB`, `PubChem`, `KEGG`, `SMILES`, `Comment`
- `Comment=0` 表示未匹配的物质（工具会处理这些行）
- `Comment=1` 表示已匹配的物质（工具会保留不变）

---

### 步骤3: 运行匹配程序

#### 方法A：基本用法（推荐新手）

```bash
python main.py name_map.csv
```

**这个命令会**：
1. 读取 `name_map.csv`
2. 自动筛选出所有未匹配的代谢物（`Comment=0`）
3. 对每个未匹配物质：
   - 在PubChem数据库搜索候选
   - 调用DeepSeek AI选择最佳匹配
4. 自动生成输出文件：`name_map_matched.csv`

---

#### 方法B：指定输出文件名

```bash
python main.py name_map.csv -o result.csv
```

输出文件会保存为 `result.csv`

---

#### 方法C：提供代谢通路提示（提高准确性）

如果你知道这些代谢物属于某个代谢通路，可以提供提示：

```bash
python main.py name_map.csv --pathway "Glycolysis"
```

**常见代谢通路名称示例**：
- `Glycolysis`（糖酵解）
- `TCA Cycle`（三羧酸循环）
- `Fatty Acid Metabolism`（脂肪酸代谢）
- `Amino Acid Metabolism`（氨基酸代谢）

---

#### 方法D：提供预期物质列表

如果你知道可能是哪些物质，可以提供列表：

```bash
python main.py name_map.csv --compounds "Glucose,Pyruvate,Lactate"
```

多个物质用**英文逗号**分隔，不要有空格。

---

### 步骤4: 查看运行结果

#### 控制台输出示例：

```
============================================================
开始代谢物智能匹配
============================================================
总共 450 条记录，其中 120 条未匹配

[1/120] 处理: Hippuristerone K
正在搜索: Hippuristerone K
  - PubChem: 找到 3 个候选
  - HMDB: 找到 0 个候选
  ✓ AI选择: Hippuristerol (来源: PubChem)

[2/120] 处理: Pectinolide C
正在搜索: Pectinolide C
  - PubChem: 找到 2 个候选
  - HMDB: 找到 0 个候选
  ✓ AI选择: Pectinolide (来源: PubChem)

...

============================================================
匹配完成 - 结果摘要
============================================================
总记录数:       450
原未匹配数:     120
新匹配成功:     85
仍未匹配:       35
匹配成功率:     70.8%
============================================================
已保存到: name_map_matched.csv
```

---

### 步骤5: 检查输出文件

打开生成的 `name_map_matched.csv`：

**匹配成功的行**：
- `Match` 列会填充标准名称
- `PubChem` 列会填充CID
- `SMILES` 列会填充化学结构
- `Comment` 从 `0` 变为 `1`

**仍未匹配的行**：
- 所有列保持 `NA`
- `Comment` 仍为 `0`

---

### 步骤6: 将结果导入MetaboAnalyst

1. 打开MetaboAnalyst网站
2. 进入 **Pathway Analysis** 模块
3. 上传修改后的 `name_map_matched.csv` 文件
4. 继续进行通路分析

---

## 🔧 常见问题

### Q1: 提示 "Module not found" 错误

**原因**：依赖包未安装

**解决**：
```bash
pip install openai requests
```

---

### Q2: 提示 "API key错误" 或 "401 Unauthorized"

**原因**：DeepSeek API密钥无效或未配置

**解决**：
1. 打开 `config.py` 文件
2. 确认 `DEEPSEEK_API_KEY` 是正确的密钥
3. 确保密钥没有过期

---

### Q3: 运行很慢

**原因**：
- 网络问题（访问PubChem或DeepSeek API慢）
- 未匹配物质太多

**解决**：
- 使用稳定的网络
- 第一次运行后会有缓存，第二次会快很多
- 可以先用少量数据测试

---

### Q4: 匹配成功率低

**原因**：
- 代谢物名称太不标准
- 数据库中确实没有这些物质

**建议**：
- 使用 `--pathway` 参数提供通路提示
- 使用 `--compounds` 参数提供预期物质列表
- 人工复核仍未匹配的物质

---

### Q5: 想清空缓存重新匹配

```bash
python main.py name_map.csv --clear-cache
```

---

## 💡 实用技巧

### 技巧1: 批量处理多个文件

```bash
# Windows
for %f in (*.csv) do python main.py %f

# Linux/Mac
for f in *.csv; do python main.py "$f"; done
```

---

### 技巧2: 只测试前几个未匹配物质

暂时不支持限制数量，如果想测试可以：
1. 复制CSV文件
2. 手动删除大部分未匹配行，只保留几行
3. 运行测试

---

### 技巧3: 查看详细帮助信息

```bash
python main.py --help
```

---

## 📞 需要帮助？

如果遇到问题：
1. 检查Python版本：`python --version`（需要3.8+）
2. 检查依赖安装：`pip list | grep openai`
3. 检查CSV文件格式
4. 查看错误日志

---

## ⚠️ 重要提醒

1. **API费用**：DeepSeek API会产生费用，大量匹配会消耗额度
2. **网络连接**：需要能访问 PubChem 和 DeepSeek API
3. **结果验证**：AI匹配不是100%准确，建议人工复核
4. **备份数据**：运行前建议备份原始CSV文件

---

## 🎯 快速命令速查表

```bash
# 基本匹配
python main.py name_map.csv

# 指定输出文件
python main.py name_map.csv -o output.csv

# 提供通路提示
python main.py name_map.csv --pathway "Glycolysis"

# 提供预期物质
python main.py name_map.csv --compounds "Glucose,Pyruvate"

# 清空缓存
python main.py name_map.csv --clear-cache

# 组合使用
python main.py name_map.csv -o result.csv --pathway "TCA Cycle" --clear-cache
```
# ⚠️ 历史说明

本文记录的是旧版命令，已停止使用。当前入口是 `python main.py run name_map.csv`，完整参数见 [README.md](README.md)。
